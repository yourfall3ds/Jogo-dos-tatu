# Segredos — nunca versionar
.env
.env.local

# Banco de dados local gerado em runtime
tools/db/

# Logs e temporários
tools/_*.log
*.log
node_modules/

# Certificados self-signed do servidor LAN (NUNCA commitar a chave privada)
certs/

# Backups dos GLBs de anim originais (com mesh) - NAO versionar (~500MB)
assets/animations/.orig-backup/

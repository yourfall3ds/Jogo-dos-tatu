# Template de variáveis de ambiente — copie para .env e preencha.
# (.env é ignorado pelo git; este .env.example pode ser versionado.)

# Chave da API do Meshy AI — https://www.meshy.ai/api-keys
MESHY_KEY=
